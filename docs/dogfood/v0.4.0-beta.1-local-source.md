# BR AI Rules V0.4 Dogfood Report（本地团队规则源）

- **日期**：2026-05-13  
- **包**：`@br-ai/rules@beta`（`0.4.0-beta.1`）  
- **仓库内团队源路径**：`dogfood-team-source/`（已加入 `.gitignore`，勿提交）  
- **模拟业务项目路径**：`dogfood-real-project/`（已加入 `.gitignore`）

## 检查项摘要

| 步骤 | 结果 |
|------|------|
| `source init` 创建 `team.starter-pack` + `team.code-review-required` | 通过 |
| `source check` | `Source OK` |
| `init --stack spring-boot,java,mysql,redis --source … --asset team.starter-pack` | 通过，`Synced 3 file(s)` |
| `check` | `All generated rule files are up to date.` |
| `doctor`（健康） | 配置、来源、合并、三目标文件、`generated.json` 均正常 |
| `source list` | 列出 `dogfood-team-source`，资产含 `team.starter-pack` |
| `asset list` | Source assets 含 `● team.starter-pack` |
| `doctor`（删除 `CLAUDE.md`） | 报告 `✗ 缺少生成目标文件: CLAUDE.md` |
| 修改源规则后 `diff` → `sync` → `check` | `diff` 有输出；`sync` 成功；`check` 再次通过 |
| `.ai-rules/config.json` / `generated.json` 顶层 `version` | 均为 `0.4.0-beta.1` |

## 命令摘录

### source init / check

```text
Created team rule source at …/dogfood-team-source/
Source OK: …/dogfood-team-source
```

### doctor（缺少 CLAUDE.md）

```text
✓ AGENTS.md 与配置一致
✗ 缺少生成目标文件: CLAUDE.md
✓ .cursor/rules/ai-coding.mdc 与配置一致
```

### 版本字段

```text
  "version": "0.4.0-beta.1",
```

（`generated.json` 内 `sources[].version` 仍为团队源 manifest 的 `0.1.0`，属预期。）

## 原始完整日志

本机执行时完整输出已写入（可复跑覆盖）：

```text
/tmp/br-ai-rules-dogfood-v04.log
```

## 后续

- **AI 是否遵守规则**：需在真实业务仓库中由人工或自动化（如 PR 中跑 `check` / CI）持续验证；本报告仅覆盖 CLI 与生成物链路。  
- **文档**：`docs/plans/br-ai-rules-v0.3-plan/` 等历史计划文件名仍保留「V0.3」字样，仅示例 JSON 中的 `version` 与当前 beta 对齐，避免复制粘贴到过旧版本号。
